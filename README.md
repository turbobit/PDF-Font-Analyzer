# PDF Font Analyzer

PDF 파일에서 사용된 폰트를 분석하고 라이선스 상태를 확인할 수 있는 웹 애플리케이션입니다.

## 주요 기능

- 📄 **PDF 폰트 분석**: PDF 파일을 업로드하여 사용된 모든 폰트를 자동으로 추출
- 🔍 **폰트 정보 확인**: 폰트 이름, 타입, 임베드 여부 등 상세 정보 제공
- 📊 **폰트 데이터베이스**: 폰트 라이선스 상태를 관리하는 데이터베이스
- ⏳ **대기 중인 폰트**: 라이선스 상태가 알려지지 않은 폰트를 관리
- 🎨 **직관적인 UI**: 모던하고 사용하기 쉬운 사용자 인터페이스

## 기술 스택

- **프레임워크**: Next.js 14 (App Router)
- **언어**: TypeScript
- **UI**: React 18
- **PDF 처리**: pdfjs-dist
- **데이터베이스**: better-sqlite3
- **스타일링**: CSS-in-JS (styled-jsx)

## 시작하기

### 필수 요구사항

- Node.js 18 이상
- npm 또는 yarn

### 설치

```bash
# 의존성 설치
npm install
```

### 개발 서버 실행

```bash
# 개발 모드로 실행 (포트 5500)
npm run dev
```

브라우저에서 [http://localhost:5500](http://localhost:5500)을 열어 애플리케이션을 확인할 수 있습니다.

### 프로덕션 빌드

```bash
# 프로덕션 빌드
npm run build

# 프로덕션 서버 실행
npm start
```

## 프로젝트 구조

```
PDF-Font-Analyzer/
├── src/
│   ├── app/                 # Next.js App Router 페이지
│   │   ├── page.tsx        # 메인 분석 페이지
│   │   ├── database/       # 폰트 데이터베이스 페이지
│   │   └── pending/        # 대기 중인 폰트 페이지
│   ├── components/         # React 컴포넌트
│   │   ├── FileUpload.tsx
│   │   ├── FontList.tsx
│   │   ├── FontDatabase.tsx
│   │   └── Navigation.tsx
│   ├── utils/              # 유틸리티 함수
│   │   ├── pdf-analyzer.ts
│   │   └── pdf-analyzer-types.ts
│   └── lib/                # 라이브러리 및 데이터베이스
│       └── db.ts
├── next.config.js          # Next.js 설정
├── package.json
└── tsconfig.json
```

## 사용 방법

1. **PDF 파일 업로드**: 메인 페이지에서 분석할 PDF 파일을 선택합니다.
2. **폰트 분석**: 애플리케이션이 자동으로 PDF에서 사용된 모든 폰트를 추출합니다.
3. **결과 확인**: 추출된 폰트 목록과 각 폰트의 라이선스 상태를 확인할 수 있습니다.
4. **데이터베이스 관리**: 폰트 데이터베이스 페이지에서 폰트의 라이선스 상태를 관리할 수 있습니다.

## API 엔드포인트

- `GET /api/font-status?name={fontName}` - 폰트의 라이선스 상태 조회
- `POST /api/add-pending-font` - 대기 중인 폰트 목록에 추가
- `GET /api/pending-fonts` - 대기 중인 폰트 목록 조회
- `GET /api/font-list` - 데이터베이스의 모든 폰트 목록 조회

## 주요 특징

### 폰트 분석 알고리즘

- **다중 전략 분석**: Operator List, commonObjs, page.objs 등 여러 방법으로 폰트를 추출
- **스마트 폰트 이름 정리**: 서브셋 접두사 제거 및 16진수 접미사 제거
- **중복 제거**: 동일한 폰트를 여러 번 감지하지 않도록 중복 제거

### 클라이언트 사이드 처리

- PDF 분석은 완전히 클라이언트 사이드에서 실행되어 서버 부하를 최소화
- `pdfjs-dist`는 동적 import를 통해 런타임에만 로드되어 초기 번들 크기 최적화

## 문제 해결

### 빌드 에러

만약 `pdfjs-dist` 관련 빌드 에러가 발생한다면:

```bash
# .next 캐시 삭제 후 재빌드
rm -rf .next
npm run build
```

### 개발 서버 재시작

개발 중 문제가 발생하면 개발 서버를 재시작하세요:

```bash
# Ctrl+C로 서버 중지 후
npm run dev
```

## 라이선스

이 프로젝트는 private 프로젝트입니다.

## 기여

이슈나 개선 사항이 있다면 이슈를 등록해주세요.

## 개발 노트

- PDF 분석은 `pdfjs-dist` 라이브러리를 사용합니다
- 폰트 데이터는 SQLite 데이터베이스에 저장됩니다
- 서버 사이드 렌더링(SSR) 문제를 방지하기 위해 `pdfjs-dist`는 클라이언트 사이드에서만 로드됩니다

